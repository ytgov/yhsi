version: "3.7"
services:
    node:
        build: 
            context: "./"
            dockerfile: Dockerfile
        # command: ["nodemon", "server.js"]
        volumes:
            - "./:/usr/src/app"
            - "./.env:/.env"
            - "./database.db:/database.db"
        ports:
            - 8888:3000
